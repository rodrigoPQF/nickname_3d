## ThreeJS Nickname Sphere

Necessário do [Node.js](https://nodejs.org/en/download/) v22.4.0.
Depois utilize esses comandos:

```bash
# Instalar as dependencias
npm install

# O Servidor ira abrir no localhost:8080
npm run dev


```
